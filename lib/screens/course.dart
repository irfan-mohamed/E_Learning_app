import 'package:flutter/material.dart';

class coursField extends StatelessWidget {
  const coursField({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SafeArea(
          child: Container(
        child: Column(
          children: [
            // video
            Container(),
            // title short title
            Container(),
            // description
            Container(),
          ],
        ),
      )),
    );
  }
}
